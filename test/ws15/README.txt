To run these test cases:
    ../../run.sh settings-full
or
    ../../run.sh settings-tiny

Either test, if it needs it, will download and install into this
directory the data that it needs (data too unwieldy for github).
